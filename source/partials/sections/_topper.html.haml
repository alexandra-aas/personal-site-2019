%section.topper
  .topper__content
    %h1
      Dedham
      %br
      Cycle
      %br
      Club
    %p.topper__subhead
      Fostering a welcoming & safe
      %br
      bike community in Dedham.
    = link_to "Learn More", "#mission", class: 'topper__cta'
  .topper__featured-event
    - featured_events = data.space.events.select{ |id, event| event.featured }
    - featured_events.each do |id, event|
      = partial 'partials/components/featured_event', locals: { event: event }
